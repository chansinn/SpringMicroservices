package com.chansin.web;

import java.io.IOException;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.web.client.RestClientException;



@SpringBootApplication
public class ChansinWebClientApplication {

	public static void main(String[] args) throws RestClientException, IOException {
		SpringApplication.run(ChansinWebClientApplication.class, args);
		/*
		 * ApplicationContext ctx
		 * =SpringApplication.run(ChansinWebClientApplication.class, args);
		 * ClientReqController reqclient = ctx.getBean(ClientReqController.class);
		 * reqclient.getEmployee();
		 */
		
	}
	
	/*
	 * @Bean ClientReqController clientRequestController() { return new
	 * ClientReqController(); }
	 */

}

